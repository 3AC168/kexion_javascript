/* function powerFuncIter(num, power){
    for (var i=0; i<5; i++);
    var i=num, power;
    return Math.pow(num, power);  // x**y = Math.pow(x, y), une puissance d'un number
}
powerFuncIter = Math.pow(3, 3);
console.log(powerFuncIter); */

function powerFuncIter(num, power){
    
    var i=num, power;
    while (i<num, power){
        i++;
    return i**i;
    }
}
powerFuncIter = 3**3;
console.log(powerFuncIter); 
