/*function powerFuncRec(num, power){
    for (var i=5; 0<i<5; i--);
                             // power = num*num => x**y = Math.pow(x, y)
    return Math.pow(num, pow);
}
powerFuncIter=Math.pow(0, 3);
console.log(powerFuncIter);*/

function powerFuncRec(num, power){
   /* for (var i=0; i<5; i--);*/
    if (num=0, power=0);
    return;
    /*console.log('hello');*/
    return (i-1)**(i-1);
    
}
powerFuncRec = 3;
console.log(powerFuncRec);